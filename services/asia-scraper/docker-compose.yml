version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: asia-lottery-postgres
    environment:
      POSTGRES_DB: asia_lottery
      POSTGRES_USER: lottery_user
      POSTGRES_PASSWORD: lottery_pass
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lottery_user"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: asia-lottery-redis
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  scraper:
    build: .
    container_name: asia-lottery-scraper
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://lottery_user:lottery_pass@postgres:5432/asia_lottery
      REDIS_URL: redis://redis:6379/0
      API_SECRET_KEY: change-me-in-production
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
